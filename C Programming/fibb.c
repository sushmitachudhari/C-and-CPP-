#include<stdio.h>
int main()
{
	void fib(int ,int,int );
	int x=1,y=1,num;
	printf("\n Number upto Fibbonacci Series");
	scanf("%d",&num);
	fib(x,y,)
}
   void fib(int a,int b,int n)
   {
   	   int sum=0;
   	   while(sum==n)
   	   {
	   }
   	   
   }
